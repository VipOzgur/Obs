@model IEnumerable<Fakulteler>
@{
    ViewData["Title"] = "Fakulteler";
}

<div class="page-heading">
    <div>
        <h1 class="h3 mb-1">Fakulteler</h1>
        <p class="text-muted mb-0">Fakulte bilgileri ve kurulus tarihleri.</p>
    </div>
    @if (User.IsInRole("SuperAdmin"))
    {
        <a class="btn btn-success" asp-action="Create">Yeni fakulte</a>
    }
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-info">@TempData["SuccessMessage"]</div>
}

<div class="surface">
    <div class="table-responsive">
        <table class="table align-middle mb-0">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Fakulte</th>
                    <th>Adres</th>
                    <th>Kurulus</th>
                    <th class="text-end">Islemler</th>
                </tr>
            </thead>
            <tbody>
            @if (Model?.Any() == true)
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td class="fw-semibold">@item.ad</td>
                        <td>@item.adres</td>
                        <td>@item.kurulusTarihi.ToString("dd.MM.yyyy")</td>
                        <td class="text-end">
                            @if (User.IsInRole("SuperAdmin"))
                            {
                                <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id">Duzenle</a>
                                <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id" onclick="return confirm('Bu fakulte silinsin mi?');">Sil</a>
                            }
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr><td colspan="5"><div class="empty-state">Henuz fakulte kaydi yok.</div></td></tr>
            }
            </tbody>
        </table>
    </div>
</div>
